using System;
namespace OWCE
{
    public class AppConstants
    {
        public const string SyncfusionLicense = "";

        // Crash/error telemetry (see #26). Leave empty to disable - Sentry
        // initialization is skipped entirely when this is blank. Fill in with a DSN
        // from your own Sentry project (https://sentry.io) to enable it.
        public const string SentryDsn = "";
    }
}
